{
  "name": "tradesight",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "start": "node --env-file-if-exists=.env server.js",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
